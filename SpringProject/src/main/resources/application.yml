spring:
  application:
    name: SpringProject
  datasource:
    platform: mysql
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/conferenceFinder?autoreconnect=true
    password: password
    username: root
flyway:
  url: jdbc:mysql://localhost:3306/mysql
  schemas: conferenceFinder
  user: root
  password: password
  locations: db/migration