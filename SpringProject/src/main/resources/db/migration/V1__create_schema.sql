CREATE TABLE conference (
id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
conference_name VARCHAR(255),
conference_date_time DATETIME,
city VARCHAR(255),
conference_description TEXT,
conference_topic VARCHAR(255)
);


